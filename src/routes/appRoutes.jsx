import React from "react";
import {  Routes, Route } from "react-router";

export const AppRoutes = () => {
  return (
    <Routes>
      <Route path="/dashboard" element={<>dasboarggg</>} />
      <Route path="/profile" element={<>perfil</>} />
    </Routes>
  );
};
